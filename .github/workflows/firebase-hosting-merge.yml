# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
env:
  REACT_APP_API_FB_KEY: ${{ secrets.REACT_APP_API_KEY }}
  REACT_APP_FB_AUTH_DOMAIN: ${{ secrets.REACT_APP_AUTH_DOMAIN }}
  REACT_APP_FB_DATABASE_URL: ${{ secrets.REACT_APP_DATABASE_URL }}
  REACT_APP_FB_PROJECT_ID: ${{ secrets.REACT_APP_PROJECT_ID }}
  REACT_APP_FB_STORAGE_BUCKET: ${{ secrets.REACT_APP_STORAGE_BUCKET }}
  REACT_APP_FB_MESSAGING_SENDER_ID: ${{ secrets.REACT_APP_MESSAGING_SENDER_ID }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: CI=false && npm install && npm run build
      
      - name: List env
        run: env
        
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          APP_API_KEY: "${{ secrets.APP_API_KEY }}"
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_MONEY_TRACKER_82CAE }}"
          channelId: live
          projectId: money-tracker-82cae
